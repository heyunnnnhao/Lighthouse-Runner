const { makeJson, formatTime } = require('./utilities');
const { getAverage, getStandardDeviation } = require('./statistics');
const {} = require('./buffer');

module.exports = { makeJson, getAverage, getStandardDeviation, formatTime };
